import React from 'react';
import {View, Text, Platform, StyleSheet} from 'react-native'

const Header = (props) => {
  return (
    <View>
      <Text>Image Hunt</Text>
    </View>
  );
};

export default Header;